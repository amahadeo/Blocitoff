<%= content_tag :tr, id: "item-#{item.id}" do %>
  <td><%= item.name %></td>
  <td><%= item.days_left %></td>
  <td><%= link_to "", [item.user, item], method: :delete, class: 'glyphicon glyphicon-ok', id: "deleteitem-#{item.id}", remote: true %></td>
<% end %>