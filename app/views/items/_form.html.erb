<tr id='form-row'>
  <td>
    <%= form_for [user, item] do |f| %>
      <div class="row">
        <div class="col-md-8">
        <%= f.text_field :name, class: "form-control", placeholder: "Create new todo" %>
        </div>
        <%= f.submit "Create", class: 'btn btn-success' %>
      </div>
    <% end %>
  </td>
  <td></td>
  <td></td>
</tr>